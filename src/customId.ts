import { CustomId } from "@trixis/lib-ts-bot";

export const removeCategorySelectMenuCustomId = new CustomId(
  "removeCategorySelectMenuCustomId"
);

export const selectCategorySelectMenuCustomId = new CustomId(
  "selectCategorySelectMenuCustomId"
);
